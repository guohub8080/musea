import { BookOpen } from "lucide-react";
import type { CategoryInfo } from "../data/tonicmlScoreLoader.tsx";

const info: CategoryInfo = {
  icon: BookOpen,
  slug: "intro",
};

export default info;

