import { BookOpen } from "lucide-react";
import type { CategoryInfo } from "../data/tonicmlDocumentLoader.tsx";

const info: CategoryInfo = {
  icon: BookOpen,
  slug: "intro",
};

export default info;

